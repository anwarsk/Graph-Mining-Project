package db.json;

import result.Result;

public class JSONAccessLayer {

	Result readResultFromJSONFile(String jsonFile)
	{
		assert jsonFile.isEmpty() == false : "Empty file path";
		
		Result result = null;
		
		
		
		return result;
	}
}
